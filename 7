    <!-- Profile Button at the Bottom -->
    <div class="list-group list-group-flush mx-3 mt-auto">
      <p
        class="mb-2 list-group-item list-group-item-action py-2 ripple"
        (click)="openProfile()"
      >
        <mat-icon>person</mat-icon>
        <span class="d-none d-md-inline">Profile</span>
      </p>
    </div>


<nav id="sidebarMenu" class="collapse d-lg-block sidebar collapse bg-white">
  <div class="d-flex flex-column h-100">
    <div class="list-group list-group-flush mx-3 mt-4">
      <p
        class="mb-2 list-group-item list-group-item-action py-2 ripple"
        (click)="homeNavigate()"
      >
        <mat-icon>home</mat-icon>
        <span class="d-none d-md-inline">Dashboard</span>
      </p>

      <p
        *ngIf="userName === 'Admin User' || userName === 'Write User'"
        class="mb-2 list-group-item list-group-item-action py-2 ripple"
        (click)="openCreate()"
      >
        <mat-icon>add</mat-icon>
        <span class="d-none d-md-inline">Create</span>
      </p>

      <p
        *ngIf="userName === 'Admin User'"
        class="mb-2 list-group-item list-group-item-action py-2 ripple"
        (click)="userAccess()"
      >
        <mat-icon>people</mat-icon>
        <span class="d-none d-md-inline">User Access</span>
      </p>
    </div>

    <!-- Spacer to push the Profile button to the bottom -->
    <div class="flex-grow-1"></div>

    <!-- Profile Button at the Bottom -->
    <div class="list-group list-group-flush mx-3 mb-4">
      <p
        class="mb-2 list-group-item list-group-item-action py-2 ripple"
        (click)="openProfile()"
      >
        <mat-icon>person</mat-icon>
        <span class="d-none d-md-inline">Profile</span>
      </p>
    </div>
  </div>
</nav>

<router-outlet></router-outlet>





  <!-- About Creator Button at the Bottom -->
  <div class="about-creator-btn mt-auto mx-3">
    <p class="list-group-item list-group-item-action py-2 ripple" (click)="openAboutCreator()">
      <mat-icon>info</mat-icon>
      <span>About Creator</span>
    </p>
  </div>



  .about-creator-btn {
  margin-top: auto; /* Push the button to the bottom */
  padding-bottom: 16px; /* Add some padding at the bottom */
  text-align: center;
}



.about-creator-btn .list-group-item {
  border-radius: 5px;
  background-color: #007bff; /* Blue color for the button */
  color: white; /* White text color */
  box-shadow: 0 2px 5px 0 rgb(0 0 0 / 16%), 0 2px 10px 0 rgb(0 0 0 / 12%);
  text-align: center;
}

.about-creator-btn .list-group-item:hover {
  background-color: #0056b3; /* Darker blue on hover */
}
